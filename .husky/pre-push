#!/usr/bin/env sh
# Whole-repo pass: catches drift on files the staged pass never saw, which is
# exactly what the type-aware rules are for.

bunx --bun oxlint --deny-warnings || exit 1
bunx oxfmt --check || exit 1
